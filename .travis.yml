language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm  

before_script:
 - composer install

script:
  - (phpunit --coverage-clover ./build/logs/clover.xml --exclude-group Functional)
  
matrix:
  allow_failures:
    - php: hhvm 
